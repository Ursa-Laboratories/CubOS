# 11 — Docs accuracy + repo hygiene (docs describe the code that exists)

Read `progress/2026-07-07-audit/00-INDEX.md` "Ground rules" first. Repo `/Users/alexchan/Documents/Ursa/CubOS`, `venv/bin/python`, no commits. The working tree has **uncommitted in-flight docs edits** (docs/calibration.md, docs/gantry.md, docs/getting-started.md, docs/protocol-yaml.md, mkdocs.yml, README.md + untracked docs/gantry-setup.md, docs/images/cubos-demo.gif) — build on top of them, never revert.

## Fixes (all verified)

1. **HIGH — ARCHITECTURE.md describes an unshipped simulator as current reality.** It documents `simulator_viewer/` (`ARCHITECTURE.md:95-104`), `backend:`/`sim_profile:` gantry YAML fields (`:23-30`), and a `Mill.connect()` backend branch (`:15-19`) — none exist: `src/gantry/simulator/` holds only stale `.pyc` files from a deleted checkout, `GantryYamlSchema` is `extra="forbid"` with no `backend` field, and `driver.py:203-211` has no backend hook. Fix: move the content to `docs/design/virtual-grbl-simulator.md` with a prominent "Design proposal — NOT implemented" banner (rewrite present-tense claims as proposal language), delete the root `ARCHITECTURE.md`, and add the design doc to mkdocs nav under a Design/Proposals section (or leave it out of nav — your call, but it must not read as shipped behavior anywhere).
2. **HIGH — README quick-start commands fail as written.** `README.md:37`, `:59` reference `configs/protocol/asmi_move_a1.yaml` and `:127` references `configs/protocol/asmi_indentation.yaml`; the real files are `configs/protocol/asmi/move_a1.yaml` and `configs/protocol/asmi/indentation.yaml`. Fix the three paths, then verify EVERY command in README against the repo (paths, flags, script names) and fix any other rot you find.
3. **MEDIUM — three broken anchors introduced by the in-flight diff.** `docs/gantry-setup.md`'s actual headings are `## CUB vs CUB XL Seeds` and `## Define Instruments`, but `docs/getting-started.md:126` and the new docs/protocol-yaml.md "Instruments" section link `gantry-setup.md#defining-instrument-drivers`, and docs/calibration.md (two places) links `gantry-setup.md#choose-the-right-seed-cub-vs-cub-xl`. Fix the anchors or rename the headings — then make `mkdocs build --strict` part of your gate.
4. **MEDIUM — ~150 lines of instrument-driver reference were deleted, not moved.** The in-flight protocol-yaml.md edit removes the "Defining Instrument Drivers" material (required fields `type`/`vendor`/`offset_x`/`offset_y`/`depth`, the extra-fields passthrough rule, external-registry registration via `cubos.instrument_registries` entry points / `CUBOS_INSTRUMENT_REGISTRY_PATHS` (check exact env var name in `src/instruments/registry.py`) and 8 worked per-vendor YAML examples) and claims it now lives in gantry-setup.md, but `docs/gantry-setup.md:33-55` has only one minimal asmi example. Fix: reconstruct the section in gantry-setup.md from `git show HEAD:docs/protocol-yaml.md` (the pre-edit content) updated to current code. Note: if task 07 landed extras validation, update the passthrough-rule text accordingly.
5. **LOW — stage the untracked files the diff depends on**: `git add docs/gantry-setup.md docs/images/cubos-demo.gif` (stage only — no commit) so they can't be lost when the user commits.
6. **LOW — dead bytecode ghosts.** `src/gantry/simulator/` and `src/board/` contain ONLY `__pycache__` (.pyc from deleted sources; nothing imports either — verify with grep before deleting). Remove both directories.
7. **LOW — configs hygiene.** `configs/deck/panda-deck.yaml` vs `configs/deck/panda_deck.yaml`: near-identical names, different contents; `configs/README.md` documents only `panda_deck.yaml`. Diff them; delete the undocumented orphan (or, if it's clearly a distinct useful example, rename it descriptively and document it). Also `configs/README.md` claims "when `tip_length` is omitted, CubOS defaults to 59.3 mm" — false: `TipRack.tip_length` is required with "no safe default" (`src/deck/labware/tip_rack.py:85-93`). Fix the README.
8. **LOW — docs CI.** `.github/` exists — check whether any workflow builds the docs; if not, add a minimal workflow (or extend the existing test workflow) running `pip install -e '.[docs]' && mkdocs build --strict` so broken anchors can't ship again.

## Gate

`venv/bin/python -m mkdocs build --strict` clean (install `.[docs]` extras into the venv if needed); `venv/bin/python -m pytest -q` still green (you shouldn't touch code, but the deletions in 6 must not break imports); manually re-run the three fixed README commands' validation variant: `venv/bin/python setup/validate_setup.py configs/gantry/cub_asmi.yaml configs/deck/asmi_deck.yaml configs/protocol/asmi/move_a1.yaml` (adjust to whatever the README actually instructs) and confirm PASS.
