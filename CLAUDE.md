Read `AGENTS.md` first and follow its safety, retrieval, progress-note, and validation rules.

Default behavior:
- Use TDD for implementation unless `AGENTS.md` debugging mode applies.
- Keep code clean, run focused tests as you work, and delete temporary files before finalizing.
- In planning mode, ask follow-up questions and wait for plan review before executing.
- Update durable docs only for user-visible workflow/API/schema/config/coordinate/protocol changes.
