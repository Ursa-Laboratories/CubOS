# WellPlateHolder (deck.labware.well_plate_holder.WellPlateHolder) —
# class-attribute template for the conductive variant (originally the
# SlideHolder_Top geometry shipped as the cubos class default).
#
# Every key below maps directly to a field on the WellPlateHolder pydantic
# class. `location` is deliberately omitted; deck YAMLs supply it per-instance.

type: well_plate_holder
name: wellplate_holder_conductive
model_name: SlideHolder_Top
length: 100.0
width: 155.0
height: 14.8
labware_support_height: 10.0
labware_seat_height_from_bottom: 5.0
